# ============================================================
#  CipherOps Redis Configuration
#  INTENTIONALLY INSECURE – CTF LAB ONLY
# ============================================================

# MISCONFIGURATION 1: bind to all interfaces instead of 127.0.0.1
bind 0.0.0.0

# MISCONFIGURATION 2: disable protected-mode (allows unauthenticated remote access)
protected-mode no

# MISCONFIGURATION 3: no password (requirepass disabled)
# requirepass <not set>

# Allow CONFIG SET so players can abuse:
#   CONFIG SET dir /root/.ssh
#   CONFIG SET dbfilename authorized_keys
#   SAVE
# (Redis-to-RCE via SSH key injection technique)

loglevel notice
databases 16

# RDB persistence enabled (needed for the SAVE-based key injection technique)
save 900 1
save 300 10
save 60 10000
